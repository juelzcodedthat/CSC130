total = 0

num = input('Enter a number or "finish" to stop: ')

while num != 'finish':
    total = total + int(num)
    num = input('Enter a number or "finish" to stop: ')

print(f'The total is {total}')
